<%= MultiJson.dump({
  message_template: {
    sender:             sender,
    body:               body,
    name:               name,
    delivery_mechanism: delivery_mechanism,
    profile_id:         profile_id,
    subject:            subject,
    format:             email_format
  }
}) %>
